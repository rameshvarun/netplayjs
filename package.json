{
    "name": "netplayjs-workspace",
    "workspaces": [
        "netplayjs-client",
        "netplayjs-demo"
    ],
    "scripts": {
        "test": "npm run test --workspaces",
        "format": "npm run format --workspaces"
    }
}